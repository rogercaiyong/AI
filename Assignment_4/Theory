1. python中可以使用`import`导入哪些对象？
答：Python可以通过import导入内置模块，自定义模块和包。可以导入包里面的部分模块，也可以导入模块里面的部分函数。

2. 机器学习和函数/模块的思想有哪些异同？
答：两者都是为了解决具体的问题。不同的是函数和模块的思想是有输入，通过已经定义好的算法得到输出；机器学习是通过已知的输入输出，得到中间的算法，然后根据输入和算法得到更多输出。
